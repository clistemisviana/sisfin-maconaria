#!/bin/sh
# Roda o validador antes de cada "git push".
# Se achar erro de sintaxe no index.html, o push e' abortado.
echo "→ validando antes do push..."
node "$(git rev-parse --show-toplevel)/tools/validate.mjs" || {
  echo ""
  echo "✗ push abortado: corrija os problemas acima."
  exit 1
}
